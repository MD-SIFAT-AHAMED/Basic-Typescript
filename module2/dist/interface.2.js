"use strict";
{
    const User = {
        name: "sifat ahamed",
        age: 90,
        role: "Admin"
    };
    const user2 = {
        name: "sifat",
        age: 90,
        role: "manager"
    };
    const rollNumber1 = [10, 20, 30];
    const rollNumber2 = [50, 60, 70];
    const add = (num1, num2) => num1 + num2;
    const StringAdd = (num1, num2) => num1 + num2;
}
